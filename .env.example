# Copy this file to .env.local (gitignored) and fill in real values to bake
# default GitHub Gist sync credentials into the build. See src/context/RecordsContext.jsx
# and src/api/githubGist.js for how these are used.
VITE_DEFAULT_GIST_TOKEN=
VITE_DEFAULT_GIST_ID=
